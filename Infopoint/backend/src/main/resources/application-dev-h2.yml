cockpit:
  base-url: http://localhost:8080       # URL eurer Cockpit-Instanz
  token: "API-c603802aed1873dbb9ad600f771780aff83a085e"            # API Token (Read-Only reicht)
  timeout-seconds: 10
  # endpoint-pfade optional überschreibbar falls eure Cockpit-Version anders ist:
  endpoints:
    news: "/api/collections/get/news"
    events: "/api/collections/get/events"
    map: "/api/singletons/get/map"
    assets: "/api/assets"     # nur wenn ihr Asset-Metadaten braucht
